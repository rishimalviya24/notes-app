<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Important Tasks</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.4.0/fonts/remixicon.css" rel="stylesheet"/>
</head>
<body class="bg-zinc-800 text-gray-300 font-sans h-screen flex">
  <aside class="w-1/5 bg-zinc-900 p-6 flex flex-col">
    <div class="mb-6 flex items-center gap-2">
      <div class="bg-blue-400 text-white font-bold w-12 h-12 rounded-full flex items-center justify-center">RM</div>
      <div class="flex flex-col">
        <h1 class="text-white mt-2">Rishi Malviya</h1>
        <p class="text-zinc-400 text-xs">rishimalviya615@gmail.com</p>
      </div>
    </div>
    <nav class="space-y-1">
      <button class="w-full text-left py-2 px-3 text-sm flex gap-3 hover:bg-gray-700 rounded-md">
        <i class="ri-sun-line text-white text-md"></i><span class="text-white">My Day</span>
      </button>
      <button class="w-full text-left py-2 px-3 text-sm flex gap-3 bg-gray-700 rounded-md">
        <i class="ri-star-line text-pink-500"></i><span class="text-pink-500">Important</span>
      </button>
    </nav>
  </aside>
  <main class="w-4/5 p-8 bg-black">
    <h2 class="text-2xl text-pink-500 mb-6">⭐ Important</h2>
    <form class="flex items-center gap-4 mb-6" action="/important/create" method="post">
      <input type="text" name="title" placeholder="Add an important task" class="w-full px-4 py-2 bg-gray-700 placeholder-white rounded-lg outline-none text-white" required />
      <button type="submit" class="bg-pink-500 text-white px-4 py-2 rounded hover:bg-pink-600">Add</button>
    </form>
    <div class="space-y-4">
      <% importantTasks.forEach(task => { %>
        <div class="flex items-center bg-gray-800 p-3 rounded-md">
          <i class="ri-checkbox-blank-circle-line text-pink-500"></i>
          <span class="text-white ml-3"><%= task.title %></span>
          <form action="/important/delete/<%= task.id %>" method="post" class="ml-auto">
            <button type="submit" class="text-red-500 hover:text-red-600">
              <i class="ri-delete-bin-line"></i>
            </button>
          </form>
        </div>
      <% }); %>
    </div>
  </main>
</body>
</html>
