<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-800 text-white font-sans h-screen flex justify-center items-center">
  <div class="w-full max-w-md bg-gray-900 p-6 rounded-lg">
    <h2 class="text-center text-2xl mb-6">Edit Task</h2>
    <form action="/update/<%= task.id %>" method="POST">
      <input
        type="text"
        name="title"
        value="<%= task.title %>"
        class="w-full px-4 py-2 mb-4 bg-gray-700 placeholder-white rounded-lg outline-none text-white"
        required
      />
      <button type="submit" class="w-full bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
        Update Task
      </button>
    </form>
    <a href="/" class="text-center block mt-4 text-blue-400 hover:underline">Cancel</a>
  </div>
</body>
</html>
